@extends('layouts.app') @section('content')
<main class="legal-page"><div class="legal-admin-head"><a class="brand" href="{{ route('trips.index') }}"><span>✦</span> roamly</a><a href="{{ route('trips.index') }}">Cancel</a></div><div class="editor-card trip-form-card"><p class="kicker">NEW JOURNEY</p><h1>Create a trip</h1><p class="sub">Set up the basics now. You can add expenses and members next.</p>@if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('trips.store') }}">@csrf<label>Trip name<input name="trip_name" value="{{ old('trip_name') }}" placeholder="Goa weekend" required></label><label>Group name<input name="group_name" value="{{ old('group_name') }}" placeholder="Ankur & friends" required></label><label>Description<textarea name="description" rows="4" placeholder="What is this trip about?">{{ old('description') }}</textarea></label><div class="two-fields"><label>Start date<input type="date" name="start_date" value="{{ old('start_date') }}"></label><label>End date<input type="date" name="end_date" value="{{ old('end_date') }}"></label></div><label>Currency symbol<input name="currency_symbol" value="{{ old('currency_symbol', '₹') }}" maxlength="5" required></label><button class="primary" type="submit">Create trip <span>→</span></button></form></div></main>@endsection
