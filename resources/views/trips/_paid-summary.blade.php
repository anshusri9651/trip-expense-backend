<div class="workspace-card paid-summary"><div class="card-head"><div><p class="kicker">PAYMENT BREAKDOWN</p><h2>Who paid what</h2></div><strong class="summary-total">{{ $trip->currency_symbol }}{{ number_format($trip->expenses->sum('amount'), 2) }}</strong></div><p class="summary-caption">Total trip expenses paid by each member.</p><div class="paid-grid">@foreach ($trip->members as $member)<div class="paid-row"><span class="avatar-small" style="background:{{ $member->avatar_color }}">{{ $member->initials }}</span><div><strong>{{ $member->name }}</strong><small>{{ $member->expensesPaid->count() }} {{ $member->expensesPaid->count() === 1 ? 'expense' : 'expenses' }}</small></div><b>{{ $trip->currency_symbol }}{{ number_format($member->expensesPaid->sum('amount'), 2) }}</b></div>@endforeach</div></div>
