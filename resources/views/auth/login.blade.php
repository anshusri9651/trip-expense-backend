@extends('layouts.app')
@section('content')
<main class="auth-page"><div class="auth-art"><a class="brand" href="{{ route('login') }}"><span>✦</span> roamly</a><div class="art-copy"><p class="kicker">PLAN LESS. LIVE MORE.</p><h1>Every trip<br><em>worth remembering.</em></h1><p>Keep the journey joyful and the expenses simple.</p></div></div><section class="auth-panel"><div class="form-wrap"><p class="kicker">WELCOME BACK</p><h2>Let’s get moving.</h2><p class="sub">Sign in to keep your next adventure on track.</p>@if(session('status'))<div class="alert success">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('login.store') }}">@csrf<label>Email address<input type="email" name="email" value="{{ old('email') }}" autocomplete="email" required autofocus></label><label>Password<span class="password-field"><input type="password" name="password" autocomplete="current-password" required><button type="button" class="password-toggle" aria-label="Show password" aria-pressed="false"><svg class="icon-eye" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M2.062 12.348a1 1 0 0 1 0-.696 10.75 10.75 0 0 1 19.876 0 1 1 0 0 1 0 .696 10.75 10.75 0 0 1-19.876 0Z"/><circle cx="12" cy="12" r="3"/><path class="eye-slash" d="m3 3 18 18"/></svg></button></span></label><div class="form-row"><label class="check"><input type="checkbox" name="remember" value="1" @checked(old('remember'))> <span>Remember me</span></label></div><button class="primary" type="submit">Sign in <span>→</span></button></form><p class="switch">New to roamly? <a href="{{ route('register') }}">Create an account</a></p></div></section></main>
@endsection
