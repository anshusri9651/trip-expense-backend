@extends('layouts.app')
@section('content')
<main class="auth-page"><div class="auth-art"><a class="brand" href="{{ route('login') }}"><span>✦</span> roamly</a><div class="art-copy"><p class="kicker">SECURE YOUR SPACE</p><h1>Choose a<br><em>new password.</em></h1><p>Use a strong password you do not use elsewhere.</p></div></div><section class="auth-panel"><div class="form-wrap"><p class="kicker">NEW PASSWORD</p><h2>Reset password.</h2><p class="sub">Your new password must be at least 8 characters.</p>@if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('password.update') }}">@csrf<input type="hidden" name="token" value="{{ $token }}"><label>Email address<input type="email" name="email" value="{{ old('email', $email) }}" required autofocus></label><label>New password<span class="password-field"><input type="password" name="password" required minlength="8"><button type="button" class="password-toggle" aria-label="Show password">◉</button></span></label><label>Confirm password<span class="password-field"><input type="password" name="password_confirmation" required minlength="8"><button type="button" class="password-toggle" aria-label="Show password">◉</button></span></label><button class="primary" type="submit">Reset password <span>→</span></button></form><p class="switch"><a href="{{ route('login') }}">← Back to sign in</a></p></div></section></main>
@endsection
