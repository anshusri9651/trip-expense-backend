@extends('layouts.app')
@section('content')
<main class="auth-page"><div class="auth-art"><a class="brand" href="{{ route('login') }}"><span>✦</span> roamly</a><div class="art-copy"><p class="kicker">A FRESH START</p><h1>Back to<br><em>the adventure.</em></h1><p>We’ll help you get back into your travel space securely.</p></div></div><section class="auth-panel"><div class="form-wrap"><p class="kicker">RESET ACCESS</p><h2>Forgot password?</h2><p class="sub">Enter your email and we’ll send a secure reset link.</p>@if(session('status'))<div class="alert success">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('password.email') }}">@csrf<label>Email address<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><button class="primary" type="submit">Send reset link <span>→</span></button></form><p class="switch"><a href="{{ route('login') }}">← Back to sign in</a></p></div></section></main>
@endsection
