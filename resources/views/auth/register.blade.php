@extends('layouts.app')
@section('content')
<main class="auth-page"><div class="auth-art"><a class="brand" href="{{ route('login') }}"><span>✦</span> roamly</a><div class="art-copy"><p class="kicker">MAKE ROOM FOR MORE</p><h1>Your people.<br><em>Your places.</em></h1><p>One clear view of every shared moment and every rupee.</p></div></div><section class="auth-panel"><div class="form-wrap"><p class="kicker">CREATE YOUR SPACE</p><h2>Start the adventure.</h2><p class="sub">A calmer way to travel together.</p>@if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('register.store') }}">@csrf<label>Your name<input type="text" name="name" value="{{ old('name') }}" required autofocus></label><label>Email address<input type="email" name="email" value="{{ old('email') }}" required></label><label>Password<span class="password-field"><input type="password" name="password" required minlength="8"><button type="button" class="password-toggle" aria-label="Show password">◉</button></span></label><label>Confirm password<span class="password-field"><input type="password" name="password_confirmation" required minlength="8"><button type="button" class="password-toggle" aria-label="Show password">◉</button></span></label><button class="primary" type="submit">Create account <span>→</span></button></form><p class="switch">Already have an account? <a href="{{ route('login') }}">Sign in</a></p></div></section></main>
@endsection
